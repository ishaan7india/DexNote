# Backend API URL
# This is the URL where your React frontend will make API requests to the backend

# For local development:
# Use this when running the backend server locally on your machine
# REACT_APP_BACKEND_URL=http://localhost:8000

# For production:
# Replace with your actual deployed backend URL (e.g., Render, Heroku, AWS, etc.)
# REACT_APP_BACKEND_URL=https://your-backend-url.onrender.com

# Current configuration (update this based on your environment):
REACT_APP_BACKEND_URL=http://localhost:8000
